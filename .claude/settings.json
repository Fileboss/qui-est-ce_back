{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD | grep -qE '\\.java$' && ./mvnw test; exit 0",
            "timeout": 240,
            "statusMessage": "Running JUnit tests on modified .java files..."
          }
        ]
      },
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD | grep -qE '\\.java$' && ./mvnw -q package -DskipTests && [ -d ../qui-est-ce_front/api-documentation ] && cp target/generated/swagger/openapi.yaml ../qui-est-ce_front/api-documentation/qui-est-ce.yaml; exit 0",
            "timeout": 180,
            "statusMessage": "Regenerating OpenAPI and syncing to front-end repo..."
          }
        ]
      }
    ]
  }
}
